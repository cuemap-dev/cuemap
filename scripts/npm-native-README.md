# CueMap native engine

This package contains the CueMap engine binary and English tokenizer for one operating-system and CPU combination. It is normally installed automatically by `cuemap-mcp`.

The `cuemap` command launches the packaged binary and configures its tokenizer asset. Set `TOKENIZER_PATH` only when you intentionally want to use a different compiled tokenizer.
